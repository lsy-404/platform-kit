# Security

Do not include access tokens, refresh tokens, cookies, private keys or production data in issues or pull requests. Report suspected credential exposure through the repository's private vulnerability reporting channel when available.

Tests use synthetic credentials and isolated temporary directories. Live provider authorization requires credentials managed by the consuming application.
